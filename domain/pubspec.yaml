name: domain
version: 0.0.1
publish_to: none
description: A new Flutter project.

environment:
  sdk: ">=2.17.1 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  core:
    path: ../core
  navigation:
    path: ../navigation

 # Features
  offline:
    path: ../features/offline
  onboarding:
    path: ../features/onboarding

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
