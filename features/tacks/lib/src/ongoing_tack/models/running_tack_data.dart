class RunningTackData {
  final bool hasRunningTack;
  final bool isLoading;
  final bool hasError;

  const RunningTackData({
    this.hasRunningTack = false,
    this.isLoading = false,
    this.hasError = false,
  });
}
